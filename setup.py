from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Use deep learning to do spatiotemporal downscaling of proxy-scale variables in order to extract paleoweather (blocking statistics) from paleoclimate (surface temperature reconstructions) ',
    author='Christina Karamperidou',
    license='MIT',
)
